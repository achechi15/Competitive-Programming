#include <bits/stdc++.h>

using namespace std;

// !NOTE: Disjoint Set Union is a data structure to model a collection of disjoint sets
// !NOTE: You can determine which set belongs to.

int main() {

    return 0;
}