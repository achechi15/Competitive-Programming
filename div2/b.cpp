#include <bits/stdc++.h>

using namespace std;

int main() {
    int t; cin >> t;
    while (t--) {
        int n, k; cin >> n >> k;
        int arr[n*2];
        for (int i = 0; i < n*2; i++) {
            cin >> arr[i];
        }
        
    }
    return 0;
}